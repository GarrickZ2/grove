{
  "$schema": "https://schema.tauri.app/config/2/schema.json",
  "identifier": "default",
  "description": "Default capabilities for Grove desktop app",
  "windows": ["main", "tray-popover"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
